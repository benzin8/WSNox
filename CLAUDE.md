# WSNox — Claude Code Instructions

## Git Workflow

При добавлении любой новой фичи:
1. Создать отдельную ветку от `main` (`git checkout -b feature/<name>`)
2. Реализовать фичу на этой ветке
3. Смержить (или запушить) в `main`
4. **Не удалять** feature-ветку после мержа — оставлять её как есть
