import ComingSoon from '../ComingSoon';

export default function FeedPanel() {
  return (
    <ComingSoon
      title="Лента событий"
      reason="Появится после event-логирования + Redis-pubsub admin:feed"
    />
  );
}
